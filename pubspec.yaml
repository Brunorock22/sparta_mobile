name: sparta_marketplace
description: A new Flutter application.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider:
  firebase_core:
  cloud_firestore:
  flushbar: ^1.10.4
  circular_bottom_navigation:
  carousel_slider:
  cupertino_icons:
  cached_network_image:
  smooth_page_indicator:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/images/

  uses-material-design: true

